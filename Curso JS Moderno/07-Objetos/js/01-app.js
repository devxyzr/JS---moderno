// Creacion de obejtos en JS -- los objetos son la pieza principal en JS

const nombre = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

// Un objeto puede agrupara todo en una sola variable.
// El objeto tiene como sintaxis de llave ( = { }) -- Conocido como objet literal
// Para asignar un valor dentro del objeto se utiliza (:) dos puntos
// Si hay varios elementos dentro del objeto se separan con una coma.

const producto = {
  nombre: "Monitor 20 pulgadas",
  precio: 300,
  disponible: true,
};

console.log(producto);

// En lugar de tener un varible se pueden tener un objeto que contenga esos mismops 3 valores.
