// Como acceder a los valores de un objeto.

const producto = {
  nombre: "Monitor 20 pulgadas",
  precio: 300,
  disponible: true,
};

console.log(producto);
console.log(producto.nombre); // Se accede al un propiedad el obejto con la sintaxis de punt.
console.log(producto["nombre"]); // Otra sintaxis menos comun ens utiliat ([""])
