// Funciones puras
// Funcioens puras son funciones que retornan un dato pero no modifican  los valores de la varibles

// Entra un numero y devuelve un numero

const duplicar = (numero) => numero * 2;

const numero1 = 20;
const resultado = duplicar(numero1);

console.log(resultado);
console.log(numero1);
