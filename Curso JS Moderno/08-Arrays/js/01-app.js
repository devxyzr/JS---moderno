// Crear arrays en JS -> los arraglos sirven para agrupar elementos del mismo tipo. Informacion relacionada.

// Los arrelgos utilizan [] y cada valor se separa por una coma (,) a difenrencia de los objetos que contienen llave y valor.

// Los arrays tienen algo llamado indices, los arreglos inican en 0 = posicion en el arreglo

const numero = [10, 20, 30]; //Manera normal de crear objetos

// Array constructor, esta dentro de una variable

const meses = new Array("enero", "febrero", "marzo");

// Un arreglo que contenga datos de todo tipo

const deTodo = ["hola", 10,true,"si", null, {nombre;"juan",trabajo: "programador"}, [1,2,3]]; // Un arreglo puede contener cualquer tipo de dato, incluido otro arreglo dentro de si mismo.