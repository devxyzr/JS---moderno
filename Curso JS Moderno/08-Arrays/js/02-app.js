//Acceder a los valores de un array

const numeros = [10, 20, 30, 40, 50, [1, 2, 3]];

console.log(numeros);
console.table(numeros); // Importante para conocer la posicion dentro del arreglo.

// Accerder al arreglo -> se accede a travez de su posicion dentro del indice

console.log(numeros[2]);
console.log(numeros[0]);
console.log(numeros[(5, 1)]);
